# OpenCore EFI for OptiPlex 3020
First, you need to use genSMBIOS to generate the serial number. Since the config.plist serial number and things are null, if you try to boot it will show up "OC device id not allowed" or some shits like that, so make sure you generate the genSMbios to make it work. <br>you may need to reconfigure gensmbios if you're going to change the macOS version.<br>this EFI basically supports every version unless tahoe, because dortania apparently does NOT support tahoe yet.(unless debug version) if dortania releases stable tahoe version, ill release the optiplex 3020 macos tahoe.<br>up to sequoia, worked on OptiPlex 3020 SFF. do not try unless your system is sff, you need to replace the usb mapping kext folder first. the USB map kext file is already published at https://github.com/varszegimarcell/Optiplex-3020-Hackintosh-OpenCore , so please refer at it to get usb mapping kext folder.<br>This EFI only supports Intel HD Graphics 4600. you may need config.plist reconfigure to work dedicated graphics.<br>Thats all we say. If you understand the warning, please download the EFI and hope on!
